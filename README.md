# Java Project — A to Z (Code Ready)

This repository contains the complete topic-wise Java roadmap.
Every topic folder already contains:
- A starter `.java` file with a `main()` method
- A `README.md` for notes/explanation

Just open the relevant `.java` file and write your code.
